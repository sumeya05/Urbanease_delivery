# TODO: Add Root Endpoint to FastAPI App

## Steps to Complete

- [x] Add a GET route for "/" in app-backend/main.py that returns a welcome message JSON response.

## Followup Steps

- [x] Test the server to ensure the root endpoint works and returns the expected response.
- [x] Verify that existing endpoints still function correctly.
